<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="RedSocial09.View.Principal">
  <StackLayout Orientation="Vertical">

    <Button Text="{Binding BuscarContactos}" Command="{Binding cmdBuscarCont}" 
            BackgroundColor="Green" TextColor="White"></Button>
    <Button Text="{Binding EnviarMensaje}" Command="{Binding cmdEnviarMsj}" 
            BackgroundColor="Navy" TextColor="White"></Button>
    
  </StackLayout>
</ContentPage>